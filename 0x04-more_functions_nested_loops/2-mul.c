/* File: 2-mul.c */

#include "main.h"

/**
 * mul- function that multiplies two integers
 * @a - First parameter
 * @b - Second parameter
 *
 * Return: multiplication result
 */
int mul(int a, int b)
{
	int z = (int)a * (int)b;

	return (z);
}
